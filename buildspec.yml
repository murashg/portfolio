version: 0.2

phases:
  build:
    commands:
      - npm install
      - npm run webpack

artifacts:
  files:
    - index.html
    - favicon.ico
    - styles/main.css
    - images/*
    - images/favicon/*
    - js/*
    - dist/bundle.js
